name: apiverve_benchmarkrate
description: Benchmark Rate is a tool for retrieving current risk-free reference rates that replaced LIBOR. It returns rates for SOFR (US), SONIA (UK), ESTR (EU), TONA (Japan), and SARON (Switzerland).
version: 1.1.13
homepage: https://apiverve.com/marketplace/benchmarkrate?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/benchmarkrate-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
